# Separate from the root requirements.txt (TensorFlow, for Tasks 1-4) since
# this module is a PyTorch + Hugging Face stack -- kept apart to avoid a
# TensorFlow/PyTorch dependency clash in the same environment.
#
# torchao is deliberately NOT pinned here: transformers/peft import it
# opportunistically and work fine without it for plain-precision inference
# (which is all CI and finetuning/generate.py need). It's only relevant to
# training (see finetuning/train_lora.py and the README for the real
# torchao version conflict hit on Google Colab's pre-provisioned image).
torch>=2.2.0
transformers>=4.44.0
peft>=0.13.0
trl>=0.10.0
accelerate>=0.33.0
datasets>=2.20.0
